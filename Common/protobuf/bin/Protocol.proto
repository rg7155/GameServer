syntax = "proto3";
package Protocol;

import "Enum.proto";
import "Struct.proto";

message C_LOGIN
{

}

message S_LOGIN
{
	bool success = 1;
	repeated Player players = 2; // 아이디 발급 전
}

message C_ENTER_GAME
{
	//
	uint64 playerIndex = 1;
}

message S_ENTER_GAME
{
	bool success = 1;
}

message C_CHAT
{
	string msg = 1;
	ChatType chatType = 2;
	uint64 channel = 3;
}

message S_CHAT
{
	string msg = 1;
	ChatType chatType = 2;
	uint64 channel = 3;
}